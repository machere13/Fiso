Fiso — Files + Sorter пока такое название

Python 3.11+.

Чтобы посмотреть версию питона

```bash
python --v
```

Внешних зависимостей пока нет, но на всякий пропиши

```bash
pip install -r requirements.txt
```

```bash
python -m app.main
```
